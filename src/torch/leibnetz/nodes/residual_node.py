from .node import Node

# defines a node that performs a residual convolution block
class ResidualNode(Node):
    def __init__(self) -> None:
        super().__init__()
        raise NotImplementedError